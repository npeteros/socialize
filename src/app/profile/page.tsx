import { AuthLayout, ProfileHeader } from "@/components/MainComponents";

export default function Profile() {
    return (
        <AuthLayout>
            <ProfileHeader />
        </AuthLayout>
    );
}
